# 리액트는 무엇인가?

## 리액트의 정의

**리액트는 JavaScript 라이브러리? 프레임워크?**

- 리액트는 `UI` 자바스크립트 라이브러리
- 싱글페이지 애플리케이션의 `UI`를 생성하는데 특화된 라이브러리
- 리액트는 `JSX` 문법과 단방향 데이터 바인딩을 사용하고 있다
- 가상돔이라는 개념을 채용해 웹 애플리케이션의 성능을 최적화한 라이브러리
- 한계점으로는 `UI`에만 집중되어있는 라이브러리 이므로 그외 추가적인 기능은 라이브러리를 추가적으로 사용해야한다.
